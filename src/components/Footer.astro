---
import { themeConfig } from '@/config';

interface Props {
  /** 固定在外壳底部时用：去掉上边距，由外层容器控制间距 */
  flush?: boolean;
}

const { flush = false } = Astro.props;
const today = new Date();
---

<footer class:list={[flush && 'flush']}>
  <div class="footer-content">
    <div class="copyright">
      <span class="year">&copy; {today.getFullYear()}</span>
      {themeConfig.site.author}
    </div>
    <div class="powered-by">
      Powered by <a href="https://astro.build">Astro</a> +
      <a href="https://github.com/the3ash/astro-chiri">Chiri</a>
    </div>
  </div>
</footer>

<style>
  footer {
    font-size: var(--font-size-s);
    font-weight: var(--font-weight-light);
    line-height: 1.75;
    color: var(--text-secondary);
    opacity: 0.75;
    margin-top: 1.5rem;
  }
  footer.flush {
    margin-top: 0;
  }
  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: nowrap;
    width: 100%;
  }
  .copyright,
  .powered-by {
    white-space: nowrap;
  }
  .copyright .year {
    opacity: 1;
  }
  footer a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s ease-out;
  }
  footer a:hover {
    color: var(--text-primary);
  }
  @media (max-width: 480px) {
    .footer-content {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.25rem;
    }
  }
</style>
